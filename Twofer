public class Twofer {
   	public static String twofer(String name) {
        if(name == null){
            name = "you";
            return "One for "+ name + ", one for me.";
        }else{
            return "One for " + name + ", one for me."; 
        }
    }
}
